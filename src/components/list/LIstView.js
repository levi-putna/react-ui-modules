import React from 'react';
import {ViewComponent} from 'components/view';

/**
 * List is a custom styled DataView which allows Grouping
 */
export default class ListView extends ViewComponent {

    static propTypes = {

    };

    static defaultProps = {

    };

    render(){
        return(
            <div>list</div>
        )
    }
}