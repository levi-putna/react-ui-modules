import React from 'react';
import {Box} from "elements/box";

/**
 * List is a custom styled DataView which allows Grouping
 */
export default class ListView extends Box {

    static propTypes = {

    };

    static defaultProps = {

    };

    render(){
        return(
            <div>list</div>
        )
    }
}